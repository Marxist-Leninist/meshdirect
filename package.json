{
  "name": "meshdirect",
  "version": "1.0.0",
  "private": true,
  "description": "Native Qwen 3.8 chat harness with direct SG1/SG2 MCP tools",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test",
    "check": "for file in server/*.js dist/assets/app.js; do node --check \"$file\" || exit 1; done && node --test"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "express": "^4.21.2"
  }
}
