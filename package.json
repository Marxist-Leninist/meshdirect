{
  "name": "meshdirect",
  "version": "1.0.0",
  "private": true,
  "description": "Native Qwen 3.8 chat harness with direct SG1/SG2 MCP tools",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test test/*.test.js",
    "check": "for file in server/*.js dist/assets/app.js dist/assets/markdown-renderer.js; do node --check \"$file\" || exit 1; done && npm test"
  },
  "engines": {
    "node": ">=20.12"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "dompurify": "3.4.13",
    "express": "^4.21.2",
    "marked": "18.0.7"
  },
  "devDependencies": {
    "jsdom": "26.1.0"
  }
}
